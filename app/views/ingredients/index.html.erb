<h1>Ingredients</h1>

<ul>
<% @ingredients.each do |ingredient|%>

<li><%= link_to ingredient.name, ingredient_path(ingredient) %> | <%= link_to "edit", edit_ingredient_path(ingredient) %> - <%= link_to "delete", ingredient_path(ingredient), method: 'delete' %></li>

<% end %>
</ul>

<p><%= link_to "Add a ingredient", new_ingredient_path %></p>